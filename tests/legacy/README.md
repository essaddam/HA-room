# Tests Legacy

Ces tests utilisent Puppeteer et sont conservés pour référence.
Les nouveaux tests doivent utiliser Playwright dans `tests/e2e/`.

Pour exécuter ces tests :
```bash
node tests/legacy/test-ha-room-integration.js